Patch, by running patch-source.sh.

First, build the docker image with build-docker.sh.

Once that's complete, edit run-docker.sh to launch docker on the host port of your choice.
Currnetly set to 8080.

run "run-docker.sh". Once launched, run "launch-inside-docker.sh" at the prompt.
